- create a grid instead of raw coordinates (x, y), then the parts/components will move and interact in it.
- random rat place is generated by get a random x, y (row, col) and check if there is no part there (free) see QtQuick snake game example